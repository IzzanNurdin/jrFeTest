"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var path = __importStar(require("path"));
/**
 * Workaround to use proper module resolution for custom rules.
 * https://github.com/palantir/tslint/issues/3436
 *
 * @param {string} name The module name.
 * @param {string} [directory] The path to the rules directory.
 */
var getRuleDirectory = function (name, directory) {
    return path.join(path.dirname(require.resolve(name)), directory || '');
};
exports.default = getRuleDirectory;
